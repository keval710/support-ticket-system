export enum Priority {
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high",
    URGENT = "urgent"
}

export enum Role {
    USER = "USER",
    ADMIN = "ADMIN"
}

export enum Provider {
    LOCAL = "local",
    GOOGLE = "google"
}

export enum LogicOperator {
    AND = "AND",
    OR = "OR"
}